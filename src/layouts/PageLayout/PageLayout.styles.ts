import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
	pageWrapper: {
		position: "relative",
		padding: "0",
		display: "flex",
		flexDirection: "column",
		minHeight: "100vh",
	},
});
